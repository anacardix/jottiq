/**
 * Pure Kotlin domain layer: models, repository interfaces, and use cases.
 *
 * Zero Android imports allowed here. Use cases are added only when they carry real logic —
 * no pass-through use cases (see CLAUDE.md).
 */
package com.anacardix.jottiq.domain
