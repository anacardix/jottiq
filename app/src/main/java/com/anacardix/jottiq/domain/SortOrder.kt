package com.anacardix.jottiq.domain

/** Note list ordering — Home's sort menu (`design/03. Sort.png`) and Settings' default. */
enum class SortOrder {
    DateEdited,
    DateCreated,
    TitleAsc,
}
