/**
 * App/folder lock gate. `BiometricPrompt` usage is confined to the `AppLockManager`
 * implementation in this package; callers depend only on the `AppLockManager` interface.
 */
package com.anacardix.jottiq.security
